modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license}"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
logoFile="logo.png"
authors="${mod_authors}"
description='''${mod_description}'''

[[dependencies.${mod_id}]]
    modId="forge"
    mandatory=true
    versionRange="${forge_version_range}"
    ordering="NONE"
    side="BOTH"
[[dependencies.${mod_id}]]
    modId="minecraft"
    mandatory=true
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"
    
[[dependencies.${mod_id}]]
    modId = "kubejs"
    mandatory = true
    versionRange = "[2001.6.5-build.24,)"
    ordering = "AFTER"
    side = "BOTH"
[[dependencies.${mod_id}]]
    modId = "rhino"
    mandatory = true
    versionRange = "[2001.2.3-build.10,)"
    ordering = "AFTER"
    side = "BOTH"

[[dependencies.${mod_id}]]
    modId = "kaleidoscope_cookery"
    mandatory = false
    versionRange = "[1.4.1,)"
    ordering = "AFTER"
    side = "BOTH"
[[dependencies.${mod_id}]]
    modId = "kaleidoscope_tavern"
    mandatory = false
    versionRange = "[1.2.0,)"
    ordering = "AFTER"
    side = "BOTH"
[[dependencies.${mod_id}]]
    modId = "kaleidoscope_chinesefood"
    mandatory = false
    versionRange = "[1.1.9,)"
    ordering = "AFTER"
    side = "BOTH"
